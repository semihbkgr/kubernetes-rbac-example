apiVersion: v1
kind: Pod
metadata:
  name: k8s-rbac-example
spec:
  serviceAccountName: example-service-account
  containers:
    - image: semihbkgr/k8s-rbas-example:latest
      name: k8s-rbac-example
      imagePullPolicy: Always
      env:
        - name: NAMESPACE
          value: default
        - name: POD_NAME
          value: example-pod
